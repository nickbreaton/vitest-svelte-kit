export enum SvelteKitModule {
    "$app/env" = "$app/env",
    "$app/navigation" = "$app/navigation",
    "$app/paths" = "$app/paths",
    "$app/stores" = "$app/stores",
    "$service-worker" = "$service-worker",
}
